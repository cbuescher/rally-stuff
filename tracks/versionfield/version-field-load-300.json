{
    "mappings": {
        "properties": {
            "my_version": {
                "type": "version"
            }
        }
    },
    "settings": {
        "index": {
            "number_of_replicas": "{{number_of_replicas | default(0)}}",
            "number_of_shards": "{{number_of_shards | default(1)}}"
        }
    }
}
